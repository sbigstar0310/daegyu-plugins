{
  "description": "Prints RULES.md into the context whenever a session starts, resumes, clears or compacts, so the working style holds in every project and survives compaction.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cat \"${CLAUDE_PLUGIN_ROOT}/RULES.md\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
